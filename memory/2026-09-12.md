# 2026-09-12

## Origin Story — Island Bum hook-first

Revisited `sh02` hook-first through `situation_resolve` + `level_speech`.

- Situation type: `intention` — own an island or live as a bum.
- Kept the loved three-beat: bet `31:52–32:04` → boats/storm `32:32–32:48` → close through `35:08`.
- Did not splice cafe-poverty. Did not recut the picture — skill already marked this splice as the one that holds.
- Re-rendered with post-concat `level_speech` (compressor + dynaudnorm + limiter + loudnorm −16 / LRA 4).
- Measured: I −16.0, TP −2.1, LRA 3.5. File: `output/deliverables/shorts/sh02_island-bum_hook-first.mp4` (64s).
- Overwrote the previous hook-first in place — user needs both files to compare. Renderer now writes `_v2` / `_v3` instead of replacing.

## Origin Story — sh03 Focus vs Tunnel

Situation: `problem` — focus becomes tunnel because you want to finish what you started.

- Setup `56:08–56:24` (built to testing / no yeses / money / stopped). Cut before `and since`.
- Hook `56:36–56:48`. Skip article `56:28–56:36`.
- Resolve `56:48–57:08` (didn't work / businesses first). Skip GAF rant.
- Contiguous failed (article in the middle, or no money/stop if you start at the hook).
- Rendered hook-first, then re-rendered: original kept, `_v2` written. Same cut — versioning test.
- Recut hook: drop first 5s (`I have been focused`). Hook is now `56:41–56:46` — “when you start with focus” → “tunnel vision”. New file `_v3`.
- `_v3` still failed situation_resolve: close was businesses-first (second story). Recut `_v4`: tunnel hook → no longer the thing / didn't work → question yourself or revive the loop. No platform/money/businesses.
- `_v4` names the loop, doesn't consolidate the method. `_v5` = detect → stop (`56:16–56:23`) → question daily + keep track (`58:38–58:56`).
- `_v5` still ended on the loop. `_v6` plays the loop as pressure, lands on “you have to keep track.”
- Locked **script before picture**: write `deliverables/shorts/{stem}.md`, wait for approval, then ffmpeg. Drafts stay in `shorts/`. Signed-off videos go to `shorts_approved/` (`--promote`).
- User approved the three remaining shorts: sh01 story-first, sh02 hook-first, sh03 hook-first → promoted.
- 2026-09-21: finished remaining shorts. sh04 WITH/OF life (hook-first). sh05 moving house contiguous `1:03:12–1:03:46`. Chunks: ch01 `04:45–19:45`; ch02 `26:10–38:00` + Tipper `45:31–48:50`.
- `_v7` pays off keep track: write a note + capture where the thought came from + different perspective (`47:48–47:52`, `47:56–48:16`). Skip make-a-video.
