# 2026-09-21

## Origin Story — finish remaining shorts + chunks

- sh04 Game WITH Life: WITH not OF (`55:32–55:40`) → prove when we don't have to (`54:48–54:56`) → everything is a game (`1:05:24–1:05:36`). 28s draft — two situations, under floor. Recut would be contiguous `54:44–55:40`.
- sh05 Moving House: winning cut `1:03:12–1:03:56` (first stream as example of the move). v1 34s out mid-clause. A-cut `1:03:04–1:03:56` (breakup) not preferred. Approved 44s still under 50s floor; user signed off.
- ch01 contiguous `04:45–19:45`. ch02 supercut `26:10–38:00` + `45:31–48:50` (skip Lebanon to fit 15 min). ch03 deferred.

## Skills from this session

- Floor 50s / prefer 55–75s. Never out mid-clause. “Right now” needs a finished example of the move.
- Script `.md` before ffmpeg. Drafts in `shorts/`, signed-off in `shorts_approved/`.
- `compare_output_path` strips `_vN` and never lands on an existing compare copy.
- After the brief: leftover-theme scan. Brief is not a cap.

## Audit — missed vs shipped

Kept: sh01 story-first, sh02 hook-first, sh05 first-stream close.
Would recut: sh03 (don't glue 47:48 capture onto tunnel), sh04 (don't splice WITH-life to gamify-close).
Missed shorts: wall/fake productivity `23:20–24:24`; capture-thoughts as own file `47:48–48:44`; 50-person interview `50:36–51:16`; presence digitized `59:00–59:52`; desire vs running `18:40–19:28` (already inside ch01).
Missed coverage: `19:50–26:10` in no deliverable; hiring `48:50–52:40` in none; ch03 still deferred.

## Entity brand (title = key)

Canonical brand files are in `brands/`. Tipper the Story brand pass (from Tipper book): keep sh05, sh08 (people/places/events), sh03, sh02, sh06, sh04; demote sh01; park sh07, sh09.

Moved to `projects/1. tipper the story/`. Transcript already complete (`00:00–1:01:56`). Wrote planner extraction from the actual transcript (Origin Story script format), not the Gemini 10-pack.

Gemini `shorts_instructions.md` copies Origin Story (5% rule at clocks that are exposure/vulnerability here). Dropped that. Nine scripts: 8 failed / dump the idea / look at what I'm building / push-pull / empathy / brute force / six months function / daily hero's journey / bootcamp at 37. Floor 50s.

## Tipper The Story — extract all shorts

User: cut from `shorts_extraction.md`, not Gemini. Wrote playback `.md` + replaced `manifest.json` shorts[]. Render `--type shorts --no-trim`. No `--promote`. No chunks.

Publish: sh05 → sh08 → sh03 → sh02 → sh06 → sh04 → sh01. Park 07/09 extracted as drafts. Clock tweaks vs extraction: sh03 hook `11:44–11:51` (keep "for the past three years", don't hang on "I have no idea why"); sh05 hook `27:44` / confession out `31:56`; sh08 hook `50:22`; sh09 setup `56:08` (don't open on "I started writing").

## Program Future Ready — transcribe (GAF 13–18)

Job folder: `projects/program future ready vids/`. Entity GAF. Six lectures in `input/`. Transcripts go per file: `output/transcript/{slug}/`.

Pipeline: `--input filename` on `process_stream.py` writes a per-video folder so the next transcribe does not overwrite. One video at a time. 13 done (Whisper) + YouTube `captions.srt`. 14–18: YouTube `.srt` as the plan script. Whisper only on a locked cut window. Two books: `output/ebook/Future-Ready.md` and `Future-Ready-Summary.md`. Spine kept as `Future-Ready-Spine.md`. Skills: `lecture_ebook` + GAF brands. A/B with Claude. Voice lock 2026-09-21: GAF = Guilds, Adventurers, and Frontiers. Stretch = ocean / buy the island. Restaurant = financially liquid. Girlfriend ships kept. No invented two-you diagram. 16 open not needed. Month = the lectures themselves. Agents = feedback loop. GAF 9–12 (his story) not in this job yet — thicken the stretch if those files drop.

## Product surface (git vs local vs website)

Deal: git = what others clone to onboard (skills, scripts, playbooks, `_template`s). Jobs, brand books, USER/MEMORY stay local. Cursor+folder is the current UI; website later talks to the same agents for shorts + chunks. Ignore rules updated. Already-tracked job/brand/memory files still sit in the index until `git rm --cached`.

## Tipper The Story — extract chunks (jobs 1+2)

Scripts (not ffmpeg): `output/deliverables/chunks/ch01_common-ground.md` supercut `03:29–05:03` + `05:34–06:23` + `07:24–17:18` (~12:17). `ch02_show-you-their-world.md` contiguous `19:47–32:34` (~12:47). Job 4 under floor (~3 min daily loop + Beirut); already sh08; do not glue job 3/5. Manifest `chapters[]` replaced Gemini Origin Story clocks. Shorts files untouched. Rendered.

## Tipper The Origin Story — transcribe then extract

- Folder: `projects/2. tipper the origin story/`. Entity **Tipper**. Source `2. Tipper _the origin_.mp4` (1:14:16). Not `0. the origin story`.
- User: write playback transcripts of the shorts and the chunks, then extract them.
- Whisper `small` started 21:29 local. No Gemini. Job-first chunks. 50s short floor.
